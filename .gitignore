# macOS
.DS_Store

# Local scratch
*.log

# leftover git temp files (safe to delete this folder)
_git_scratch_to_delete/

# --- Training data / artifacts (regenerable) ---
datasets/
*.zip
*.tgz
runs/

# Scratch from assistant sessions. One current sample report is kept in docs/;
# the rest are intermediate renders of formats that no longer exist.
Claude outputs/
